[project]
name = "bupt-edu-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.4",
]

[dependency-group]
dev = [
    "pytest>=7.4.2",
]
torch = [
    "torch>=2.0.0",“
]
